#!/usr/bin/env bash
set -e

# ─────────────────────────────────────────────────────────────
# Configuration
# ─────────────────────────────────────────────────────────────
ENV_FILE=".env"
COMPOSE_DIR="podman"
COMPOSE_FILE="${COMPOSE_DIR}/compose.yml"

echo "🚀 Deploying n8n stack"
echo "======================"

# ─────────────────────────────────────────────────────────────
# Basic checks
# ─────────────────────────────────────────────────────────────
if [[ ! -f "${ENV_FILE}" ]]; then
  echo "❌ .env file not found"
  exit 1
fi

if [[ ! -f "${COMPOSE_FILE}" ]]; then
  echo "❌ ${COMPOSE_FILE} not found"
  exit 1
fi

# ─────────────────────────────────────────────────────────────
# Load environment
# ─────────────────────────────────────────────────────────────
set -a
source "${ENV_FILE}"
set +a

echo "✅ Environment loaded"

# ─────────────────────────────────────────────────────────────
# Detect optional services
# ─────────────────────────────────────────────────────────────
SERVICES="redis n8n"

if grep -qE "^[[:space:]]*caddy:" "${COMPOSE_FILE}"; then
  SERVICES="redis n8n caddy"
  echo "🔎 Caddy detected"
fi

# ─────────────────────────────────────────────────────────────
# Start containers
# ─────────────────────────────────────────────────────────────
echo "🔄 Starting containers: ${SERVICES}"
cd "${COMPOSE_DIR}"

podman-compose down || true
podman-compose up -d ${SERVICES}

cd ..

# ─────────────────────────────────────────────────────────────
# Wait for startup (intentionally simple)
# ─────────────────────────────────────────────────────────────
echo "⏳ Waiting for containers to start..."
sleep 20

# ─────────────────────────────────────────────────────────────
# Container checks
# ─────────────────────────────────────────────────────────────
echo "🔍 Checking containers..."

for container in redis n8n; do
  if podman ps --format "{{.Names}}" | grep -q "^${container}$"; then
    echo "✅ ${container} running"
  else
    echo "❌ ${container} not running"
    podman logs "${container}" || true
    exit 1
  fi
done

# ─────────────────────────────────────────────────────────────
# Redis health check
# ─────────────────────────────────────────────────────────────
echo "🔍 Checking Redis..."
podman exec redis redis-cli -a "${REDIS_PASSWORD}" ping >/dev/null \
  && echo "✅ Redis OK" \
  || { echo "❌ Redis failed"; exit 1; }

# ─────────────────────────────────────────────────────────────
# n8n health check (local)
# ─────────────────────────────────────────────────────────────
echo "🌐 Checking n8n..."
curl -sf http://127.0.0.1:5678/healthz >/dev/null \
  && echo "✅ n8n healthy" \
  || { echo "❌ n8n unhealthy"; podman logs n8n; exit 1; }

# ─────────────────────────────────────────────────────────────
# Public check (best effort)
# ─────────────────────────────────────────────────────────────
if echo "${SERVICES}" | grep -q caddy; then
  echo "🌍 Checking public URL (best effort)..."
  curl -sf "https://n8n.${BASE_DOMAIN}/healthz" >/dev/null \
    && echo "✅ Public n8n OK" \
    || echo "⚠️ Public n8n not reachable yet"
fi

# ─────────────────────────────────────────────────────────────
# Summary
# ─────────────────────────────────────────────────────────────
echo ""
echo "🎉 Deployment complete!"
echo ""

podman ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"

echo ""
echo "🔗 Access:"
echo "  https://n8n.${BASE_DOMAIN}"
echo ""
